# Web layer
flask==3.1.0
python-dotenv==1.0.1
requests==2.32.3

# Model client — ChatOpenAI is the ONLY application-level model interface.
# The `openai` package is a transitive dependency of langchain-openai; the
# architecture test in tests/test_architecture.py enforces that no application
# module imports or constructs an OpenAI SDK client directly.
langchain-core==1.4.8
langchain-openai==1.3.3

# Token accounting
tiktoken==0.8.0

# Production server (Phase 8)
gunicorn==23.0.0; sys_platform != "win32"
waitress==3.0.2; sys_platform == "win32"

# Testing
pytest==8.3.4
